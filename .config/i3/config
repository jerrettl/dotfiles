# i3 config file (v4)
# Location: ~/.config/i3/config
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Mod1=<Alt> Mod4=<Super>
set $mod Mod4

# Don't hide borders
hide_edge_borders none

# Font
font pango:SFNS Display 10

# Use Mouse+$mod to drag windows
floating_modifier $mod

set $terminal urxvt
set $lock i3lock -B 10 --ringcolor=ccccccff --insidecolor=ffffff00 --keyhlcolor=3333ccff --bshlcolor=cc3333ff --ringvercolor=ccccccff --insidevercolor=00000000 --verifcolor=ffffffff --wrongcolor=ffffffff --linecolor=00000000 --separatorcolor=00000000 --timecolor=ffffffff --time-font="Helvetica Neue" --date-font="Helvetica Neue" --layout-font="Helvetica Neue" --verif-font="Helvetica Neue" --wrong-font="Helvetica Neue"

# The basics
bindsym $mod+Return exec $terminal
bindsym $mod+q kill
bindsym $mod+Shift+q exec $lock && sleep 1 && systemctl suspend
bindsym $mod+d exec --no-startup-id rofi -show run -font 'SFNS Display 13' -hide-scrollbar
bindsym $mod+Shift+r exec --no-startup-id bash ~/dotfiles/scripts/restart-i3.sh


# Lock mode
set $mode_system System (L) lock, (E) logout, (S) suspend, (H) hibernate, (R) reboot, (Shift+S) shutdown
bindsym $mod+Shift+e mode "$mode_system"

mode "$mode_system" {
	bindsym l exec --no-startup-id $lock, mode "default"
	bindsym e exec --no-startup-id i3-msg exit, mode "default"
	bindsym s exec --no-startup-id $lock && sleep 1 && systemctl suspend, mode "default"
	bindsym h exec --no-startup-id $lock && systemctl hibernate, mode "default"
	bindsym r exec --no-startup-id systemctl reboot, mode "default"
	bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

	bindsym Return mode "default"
	bindsym Escape mode "default"
}

# Scratchpad
# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# Blink active window
bindsym $mod+Tab exec ~/dotfiles/scripts/flash_win.sh

# Screenshot
bindsym Print exec scrot -e 'mv $f ~/Pictures/'
bindsym Shift+Print exec flameshot gui

# Shortcut applications
bindsym $mod+F1 exec chromium
bindsym $mod+Shift+F1 exec qutebrowser
bindsym $mod+F2 exec $terminal -e bash -c "cd ~/dotfiles && git pull && sleep 5"
bindsym $mod+Shift+F2 exec $terminal -e yay -Syyu --needed --norebuild
bindsym $mod+F3 exec $terminal -e ranger
bindsym $mod+Shift+F3 exec pcmanfm

# On startup
exec_always --no-startup-id feh --bg-fill ~/dotfiles/wallpaper.jpg
#exec_always --no-startup-id volumeicon
exec_always --no-startup-id nm-applet
exec_always --no-startup-id system-config-printer-applet
exec_always --no-startup-id xdotool mousemove_relative -- -10000 10000
exec_always --no-startup-id caffeine
exec_always --no-startup-id xrdb ~/.Xresources
exec_always --no-startup-id ~/dotfiles/scripts/numlockx.sh
exec_always --no-startup-id udiskie -ANs
exec_always --no-startup-id unclutter
exec_always --no-startup-id dunst
exec_always --no-startup-id setxkbmap -option compose:ralt
exec_always --no-startup-id ~/dotfiles/scripts/picom.sh
exec_always --no-startup-id ~/dotfiles/scripts/per-machine-variables.sh
exec_always --no-startup-id VBoxClient-all
exec --no-startup-id clight

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left 20px
bindsym $mod+Shift+j move down 20px
bindsym $mod+Shift+k move up 20px
bindsym $mod+Shift+l move right 20px
bindsym $mod+c move position center

# resize focused window
bindsym $mod+Ctrl+h resize shrink width 20 px
bindsym $mod+Ctrl+j resize grow height 20 px
bindsym $mod+Ctrl+k resize shrink height 20 px
bindsym $mod+Ctrl+l resize grow width 20 px

# split in horizontal or vertical orientation
bindsym $mod+g split h
bindsym $mod+v split v

bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent or child container
bindsym $mod+a focus parent
bindsym $mod+z focus child

set $workspace1 "1"
set $workspace2 "2"
set $workspace3 "3"
set $workspace4 "4"
set $workspace5 "5"
set $workspace6 "6"
set $workspace7 "7"
set $workspace8 "8"
set $workspace9 "9"
set $workspace10 "10"

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10


# move focused container to workspace and switch to it
set $move_mode Choose a workspace to move and switch to (1-10).
bindsym $mod+Shift+Tab mode "$move_mode"

mode "$move_mode" {
	bindsym 1 move container to workspace $workspace1; workspace $workspace1, mode default
	bindsym 2 move container to workspace $workspace2; workspace $workspace2, mode default
	bindsym 3 move container to workspace $workspace3; workspace $workspace3, mode default
	bindsym 4 move container to workspace $workspace4; workspace $workspace4, mode default
	bindsym 5 move container to workspace $workspace5; workspace $workspace5, mode default
	bindsym 6 move container to workspace $workspace6; workspace $workspace6, mode default
	bindsym 7 move container to workspace $workspace7; workspace $workspace7, mode default
	bindsym 8 move container to workspace $workspace8; workspace $workspace8, mode default
	bindsym 9 move container to workspace $workspace9; workspace $workspace9, mode default
	bindsym 0 move container to workspace $workspace10; workspace $workspace10, mode default

	bindsym Return mode "default"
	bindsym Escape mode "default"
}


# reload the configuration file
bindsym $mod+Shift+c reload

set $bg-color 	         #ffffff
set $inactive-bg-color   #2d2d2d
set $text-color          #000000
set $inactive-text-color #999999
set $urgent-bg-color     #d64937
set $indicator           #999999

# window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $bg-color          $text-color          $indicator
client.unfocused        $inactive-bg-color  $inactive-bg-color $inactive-text-color $indicator
client.focused_inactive $inactive-bg-color  $inactive-bg-color $inactive-text-color $indicator
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          $indicator

bar {
	status_command i3blocks

	colors{
		background #ffffff
		statusline #000000
		separator #444444

# 											border 		background 		number
		focused_workspace	 	#044f77 	#1badf8 			#000000
		active_workspace 		#444444 	#6c71c4 			#fdf6e3
		inactive_workspace 	#888888 	#ffffff 			#333333
		urgent_workspace 		#970800 	#ff3b30 			#ffffff
	}
}

# Gaps
gaps inner 0
gaps outer 0

bindsym $mod+U gaps inner current minus 4; gaps outer current minus 4
bindsym $mod+I gaps inner current plus 4; gaps outer current plus 4
bindsym $mod+O gaps inner current set 0; gaps outer current set 0
bindsym $mod+P gaps inner current set 8; gaps outer current set 8



# Poor Man's volume control
bindsym Scroll_Lock exec --no-startup-id xdotool keyup Scroll_Lock && ~/dotfiles/scripts/volume.sh down
bindsym Pause exec --no-startup-id xdotool keyup Pause && ~/dotfiles/scripts/volume.sh up
bindsym XF86AudioLowerVolume exec --no-startup-id xdotool keyup XF86AudioLowerVolume && ~/dotfiles/scripts/volume.sh down
bindsym XF86AudioRaiseVolume exec --no-startup-id xdotool keyup XF86AudioRaiseVolume && ~/dotfiles/scripts/volume.sh up
bindsym XF86AudioMute exec --no-startup-id xdotool keyup XF86AudioMute && ~/dotfiles/scripts/volume.sh toggle

# Poor Man's mouse control
bindsym $mod+Left exec xdotool mousemove_relative -- -15 0
bindsym $mod+Down exec xdotool mousemove_relative 0 15
bindsym $mod+Up exec xdotool mousemove_relative -- 0 -15
bindsym $mod+Right exec xdotool mousemove_relative 15 0

bindsym $mod+Shift+Left exec xdotool mousemove_relative -- -5 0
bindsym $mod+Shift+Down exec xdotool mousemove_relative 0 5
bindsym $mod+Shift+Up exec xdotool mousemove_relative -- 0 -5
bindsym $mod+Shift+Right exec xdotool mousemove_relative 5 0

bindsym $mod+Shift+Return exec xdotool click 1
bindsym $mod+F10 exec xdotool click 1
bindsym $mod+Ctrl+Return exec xdotool click 3
bindsym $mod+Prior exec xdotool click 4
bindsym $mod+Next exec xdotool click 5

bindsym $mod+Ctrl+0 exec --no-startup-id xdotool mousemove_relative -- -10000 10000

# Picom toggle
bindsym $mod+F8 exec --no-startup-id pkill picom
bindsym $mod+F9 exec --no-startup-id picom -b --xrender-sync-fence

# Floating window exceptions
for_window [class="Steam" instance="Steam" title="Friends"] floating enable
for_window [class="Steam" instance="Steam" title="Steam Guard - Computer Authorization Required"] floating enable
for_window [class="Firefox" title="About Mozilla Firefox"] floating enable
for_window [class="SVPManager" title="SVP - Control Panel"] floating disable

# i3-gaps fix
# for_window [class="^.*"] border pixel 0
new_window pixel 1


# Select display
set $select_display Select a display mode: (P)rimary, (S)econdary, (M)irror, or (E)xtend
bindsym $mod+F7 mode "$select_display"

mode "$select_display" {
	bindsym p exec --no-startup-id ~/dotfiles/scripts/select-monitor.sh primary, mode "default"
	bindsym s exec --no-startup-id ~/dotfiles/scripts/select-monitor.sh secondary, mode "default"
	bindsym m exec --no-startup-id ~/dotfiles/scripts/select-monitor.sh mirror, mode "default"
	bindsym e exec --no-startup-id ~/dotfiles/scripts/select-monitor.sh extend, mode "default"

	bindsym Return mode "default"
	bindsym Escape mode "default"
}


# Music mode
set $music_mode Select a music player: (P)ianobar or (S)potify
bindsym $mod+Shift+m mode "$music_mode"
bindsym $mod+Shift+p mode "$music_mode"

mode "$music_mode" {
	bindsym p exec --no-startup-id echo -n "pianobar" > ~/.musicmode, mode "default"
	bindsym s exec --no-startup-id echo -n "spotify" > ~/.musicmode, mode "default"

	bindsym Return mode "default"
	bindsym Escape mode "default"
}

# Media control bindings
bindsym $mod+M exec "~/dotfiles/scripts/music-control.sh playpause"
bindsym XF86AudioPlay exec "~/dotfiles/scripts/music-control.sh playpause"

bindsym $mod+N exec "~/dotfiles/scripts/music-control.sh next"
bindsym XF86AudioNext exec "~/dotfiles/scripts/music-control.sh next"

bindsym $mod+B exec "~/dotfiles/scripts/music-control.sh back"
bindsym XF86AudioPrev exec "~/dotfiles/scripts/music-control.sh back"

# Chrome hacks
bindsym Ctrl+grave exec --no-startup-id xdotool keyup grave && xdotool keyup ctrl && xdotool type $(cat ~/Documents/chrome-text.txt)

# Wifi selection
bindsym $mod+Shift+d exec --no-startup-id networkmanager_dmenu -fn "Helvetica Neue 12"

# Brightness
bindsym XF86MonBrightnessDown exec "xbacklight -dec 10"
bindsym XF86MonBrightnessUp exec "xbacklight -inc 10"
bindsym Shift+XF86MonBrightnessDown exec "xbacklight -dec 2"
bindsym Shift+XF86MonBrightnessUp exec "xbacklight -inc 2"

# Airplane Mode
bindsym $mod+F6 exec --no-startup-id ~/dotfiles/scripts/airplane_mode.sh
